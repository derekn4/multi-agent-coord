node_modules/

.superpowers/*
docs/*

# Coordinator runtime state (default lives at ~/.multi-agent-coord; this
# ignores any in-repo override dir a developer might point it at).
.coordinator-data/

# Local Claude Code config: machine-specific plugins and personal settings.
.claude/

node_modules/*
# Retained eval trace logs (regenerated by the runner; Phase 4 indexes these)
src/eval/traces/

# Phase 4 labeled failure corpus. Unlike src/eval/traces/ above (regenerated
# noise), this IS the index — diagnosis-baseline.json is not reproducible
# without it, so it is committed.
!src/eval/corpus/
